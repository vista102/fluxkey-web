# FluxKey Web

UI foundation for a Hall Effect keyboard web driver. This stage is a local UI demo only; hardware communication and WebHID are intentionally out of scope.

## Development

```bash
pnpm install
pnpm dev
pnpm build
```

The design reference belongs at `references/fluxkey-ui-reference.jpg` when available.
